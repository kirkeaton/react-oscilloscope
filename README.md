# react-oscilloscope

Basic example of an oscilloscope written in React

## Requirements

- Node.js >= 20

## Prerequisites

- Install dependencies by running `npm ci`

## Running

```bash
$ npm run start
```
